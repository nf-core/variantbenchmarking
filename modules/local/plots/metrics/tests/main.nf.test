nextflow_process {

    name "Test Process PLOTS_METRICS"
    script "../main.nf"
    process "PLOTS_METRICS"

    tag "modules"
    tag "modules_local"
    tag "plots_metrics"
    tag "metrics"

    test("Interactive html plots - svbenchmark benchmark") {

        when {
            process {
                """
                input[0] = [
                    [ benchmark_tool:'svbenchmark' ],
                    file(params.test_data_base + '/testdata/nf-test/plots/svbenchmark.summary.csv')
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert file(process.out.plots.get(0)).name.startsWith("variant_metrics_") },
                { assert file(process.out.plots.get(0)).name.endsWith(".html") },
                { assert snapshot(process.out.versions_python).match() }
            )
        }

    }

    test("Interactive html plots - stub") {
        options "-stub"
        when {
            process {
                """
                input[0] = [
                    [ benchmark_tool:'svbenchmark' ],
                    file(params.test_data_base + '/testdata/nf-test/plots/svbenchmark.summary.csv')
                ]
                """
            }
        }
        then {
            assertAll(
                { assert process.success },
                { assert file(process.out.plots.get(0)).name.startsWith("variant_metrics_") },
                { assert file(process.out.plots.get(0)).name.endsWith(".html") },
                { assert snapshot(process.out.versions_python).match() }
            )
        }
    }
}

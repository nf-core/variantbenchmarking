nextflow_process {

    name "Test Process PLOTS_METRICS"
    script "../main.nf"
    process "PLOTS_METRICS"

    tag "modules"
    tag "modules_local"
    tag "plots_metrics"
    tag "metrics"

    test("Standard R plots - svbenchmark benchmark") {


        when {
            process {
                """
                input[0] = [
                    [ benchmark_tool:'svbenchmark' ],
                    file(params.test_data_base + '/testdata/nf-test/plots/svbenchmark.summary.csv')
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }

    }

    test("Standard R plots - stub") {
        options "-stub"
        when {
            process {
                """
                input[0] = [
                    [ benchmark_tool:'svbenchmark' ],
                    file(params.test_data_base + '/testdata/nf-test/plots/svbenchmark.summary.csv')
                ]
                """
            }
        }
        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }
    }
}
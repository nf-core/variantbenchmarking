nextflow_process {

    name "Test Process PLOT_SVLEN_DIST"
    script "../main.nf"
    process "PLOT_SVLEN_DIST"

    tag "modules"
    tag "modules_local"
    tag "plot_svlen_dist"

    test("Standard SV length plot - VCF") {

        when {
            params {
                variant_type = 'sv'
            }
            process {
                """
                input[0] = [
                    [ id:'test_sample', tag:'manta' ],
                    file(params.test_data_base + '/testdata/hg38/test/somatic/HCC1395T_vs_HCC1395N.manta.somatic_sv.vcf.gz', checkIfExists: true)
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }
    }

    test("Standard SV length plot - stub") {

        options "-stub"

        when {
            params {
                variant_type = 'sv'
            }
            process {
                """
                input[0] = [
                    [ id:'test_stub', tag:'stub_tag' ],
                    file(params.test_data_base + '/testdata/hg38/test/somatic/HCC1395T_vs_HCC1395N.manta.somatic_sv.vcf.gz', checkIfExists: true)
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }
    }
}